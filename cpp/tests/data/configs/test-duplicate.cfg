# Duplicate parameters in the same config test

logDir = analysis_logs  # Each run of KataGo will log to a separate file in this dir
logDir = more_logs  # try duplicate
nnMaxBatchSize = 100500  # try override



