[pytest]
# Unit tests for pure-Python logic modules
# Run from the python/ directory via `pytest`
testpaths = tests
pythonpath = .
addopts = tests
norecursedirs = tmp old models testdata selfplay configs muon __pycache__ .git
